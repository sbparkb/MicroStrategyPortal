function FormatNumber(num,decimalNum,leadingZero,parenthesis){var tmpNum=num;tmpNum*=Math.pow(10,decimalNum);tmpNum=Math.floor(tmpNum);tmpNum/=Math.pow(10,decimalNum);var tmpStr=new String(tmpNum);if(!leadingZero&&num<1&&num>-1&&num!=0){if(num>0){tmpStr=tmpStr.substring(1,tmpStr.length);}else{tmpStr="-"+tmpStr.substring(2,tmpStr.length);}}if(parenthesis&&num<0){tmpStr="("+tmpStr.substring(1,tmpStr.length)+")";}return tmpStr;}