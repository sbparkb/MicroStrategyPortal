<!DOCTYPE block SYSTEM "../dtds/blockLibrary.dtd">
<block name="TitleDrillPath" inherits="BaseBlock">
	<definition>
        	<!-- drill path key -->
			<property name="k" type="String" />
        	<!-- isWithin ? -->
			<property name="within" type="Boolean" />
			<!--  drill type -->
			<property name="dt" type="Integer" />
        	<!--name ? -->
			<property name="n" type="String" />
	</definition>
</block>
